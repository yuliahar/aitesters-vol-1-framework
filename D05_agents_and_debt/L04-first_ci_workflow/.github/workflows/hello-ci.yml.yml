name: Hello from CI

on:
  push:
    branches:
      - feature/github-actions

jobs:
  greet:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Say Hello
        run: echo "Hello from CI"